/*
import './styles.scss';
import createSliderContainer from './components/Slide';

/!**
 *
 * @param {Array<object>} slides
 * @return
 *!/

export default function (slides) {
    const listContainer = document.createElement('div');
    listContainer.classList.add('slider-container');
    slides.forEach(slide => {
        listContainer.appendChild(createSliderContainer(slide));
    });


    return listContainer;
}*/
